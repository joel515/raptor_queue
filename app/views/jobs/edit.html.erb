<% provide(:title, "Edit #{@job.name}") %>
<% provide(:button_text, "Clean and resubmit") %>
<h1><%= "Edit #{@job.name}" %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'shared/job_form', control: "form-control", options: true %>
  </div>
</div>
